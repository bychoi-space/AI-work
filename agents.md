# LF Editor Studio를 위한 Antigravity 에이전트 제약 조건

## 🛠️ 기술 스택 및 아키텍처 (엄격한 규칙)
- **Vanilla JS 전용**: 프레임워크(React, Vue 등)를 절대 사용하지 마세요. 코드는 가볍고 직관적으로 유지해야 합니다.
- **단일 진실 공급원 (SSOT)**: 모든 프로젝트 상태(화면 목록, 순서, 설명 등)는 각 프로젝트 폴더의 `metadata.json`에서만 배타적으로 관리되어야 합니다.
- **3계층 모듈러 아키텍처**: 엔진 안정성을 위해 역할을 엄격히 분리합니다. 새로운 대규모 기능을 추가할 때는 기존 파일을 비대하게 만드는 대신 전용 JS 파일을 신규 생성하여 모듈화합니다.
  - **`vctrl_core.js` (Core Engine)**:
    - **역할**: 시스템의 '두뇌'. 전역 상태(`state`), `MessageHub`(창 간 통신), GitHub API 연동(저장/로드), 인라인 스크립트(`v4Script`) 관리, 스마트 가이드 계산 엔진 담당.
    - **참고**: 데이터 흐름이나 새로운 통신 프로토콜을 추가할 때 이 파일을 수정합니다.
    - **주의**: `v4Script` 수정 시 인라인 문자열의 이스케이프 처리에 극도로 주의해야 하며, 반드시 `vctrl_v4_iframe.js`와 로직의 일관성을 유지(Sync)해야 합니다.
  - **`vctrl_grouping.js` (Orchestrator Layer)**:
    - **역할**: 다중 요소 관리자. 드래그 범위 선택(Marquee), 다중 선택 상태(`selectedIds`), 그룹 이동/삭제/그룹화 로직 전담.
    - **참고**: 다중 선택 인터랙션이나 컴포넌트 간의 관계(Grouping) 기능을 추가/수정할 때 이 파일을 수정합니다.
    - **주의**: 개별 컴포넌트의 `id`와 `selected` 클래스 상태가 `vctrl_core.js`와 완벽히 동기화되어야 합니다.
  - **`vctrl_inspector.js` (UI Controller)**:
    - **역할**: 시스템의 '얼굴'. 사이드바 탭 전환, 메타데이터 입력 UI, 화면 목록 렌더링, Quill 에디터 초기화, 중앙 DOM 레지스트리 관리.
    - **참고**: 새로운 UI 컴포넌트나 인스펙터 기능을 추가할 때 이 파일을 수정합니다.
    - **주의**: `DOM` 객체에 새로운 요소를 등록할 때 `viewer.html`의 ID와 일치하는지 반드시 확인하세요. 등록된 요소는 반드시 `vctrl_core.js`에서 이벤트 리스너가 연결되어야 합니다.
  - **`vctrl_v3.js` (Utility Layer)**:
    - **역할**: 시스템의 '손'. 어노테이션 핀 생성/렌더링, 캔버스 조작(줌/팬), 장치 뷰포트 변경, 유틸리티 함수들 담당.
    - **참고**: 핀의 스타일이나 캔버스 상호작용 로직을 개선할 때 이 파일을 수정합니다.
    - **주의**: 핀 드래그 시 `SmartGuide`와의 좌표 변환(px ↔ %) 로직이 깨지지 않도록 주의하세요.
- **아톰 컴포넌트 표준 (Atomic Component Standard)**:
  - 모든 아이콘 및 아톰 컴포넌트(SVG 포함)는 인스펙터(`ICON EDITOR`)와의 호환성을 위해 반드시 **`.lf-icon`** 클래스를 포함해야 합니다.
  - SVG 아톰의 경우, 선명한 프리미엄 UI 유지를 위해 기본 `stroke-width`를 **`1.6`**으로 설정하는 것을 원칙으로 합니다.
  - **스프라이트 간섭 방지**: 새로운 SVG/커스텀 아톰에 `.lf-icon`을 적용할 경우, 반드시 `background-image: none !important;`를 적용하여 기존 시스템의 스프라이트 이미지가 노출되지 않도록 처리해야 합니다.
- **주석 (Annotations)**: 반드시 **Quill.js**를 사용하세요. 스타일 보존을 위해 주석 데이터는 `metadata.json`에 HTML 형식으로 저장해야 합니다.
- **상호작용 분리**: 마커 드래그(위치 이동)와 클릭(편집 모드 진입)은 논리적으로 완벽히 분리되어야 합니다. 특히 `.v4-editable-cell` 영역은 클릭 시 드래그 로직을 바이패스하고 즉시 `.focus()`가 호출되도록 처리해야 합니다.
- **디자인 시스템 강제화 (1.6px Border)**: 모든 V4 컴포넌트의 보더 굵기는 **1.6px**로 고정합니다. 인라인 스타일의 간섭을 막기 위해 CSS에 `!important`를 사용하고, `MutationObserver`를 통해 실시간으로 굵기를 감시 및 보정해야 합니다.

## 🎨 UI 및 컴포넌트 규칙
- **캔버스 크기**: 에디터 화면(`lf-canvas` 또는 `page`)의 크기는 레이아웃 틀어짐을 방지하기 위해 'Cover' 화면 크기(예: 1440x900)와 완벽하게 일치해야 합니다.
- **타이포그래피 가이드 (폰트 크기 표준)**: 가독성 확보를 위해 다음 기준을 따르세요.
  - 대분류 타이틀 (화면 제목 등): `18px` ~ `20px`
  - 중분류 헤더 (테이블 헤더, 그룹 제목): `15px` ~ `16px`
  - 본문 텍스트 (테이블 셀, 일반 텍스트): `14px` ~ `15px`
  - 부가 설명 및 라벨 (서브헤더, 개별 라벨, 주의사항): `13px`
  - 최소 표기 단위 (매우 작은 마커/태그 텍스트): `12px`
- **텍스트 디테일 및 정렬 (레이아웃 보호)**: 
  - 한 줄로 유지되어야 하는 텍스트(예: 날짜, 짧은 라벨)는 반드시 `white-space: nowrap;`을 적용하여 텍스트 줄바꿈(글자 잘림)을 방지하세요.
  - CSS Polygon 등의 도형 안에 텍스트를 넣을 때는 도형의 여백(Padding)과 줄간격(`line-height`)을 세밀하게 계산하여 텍스트가 도형의 한가운데에 완벽하게 정렬되도록(어색하게 쏠리지 않도록) 구성하세요.
- **컴포넌트 시스템**: 드래그 앤 드롭을 지원하기 위해 편집 가능한 모든 주요 요소는 `.lf-component`로 감싸야 합니다.
- **버튼 디자인 표준 (Premium UI)**: 모든 버튼은 [JIRA 바로가기] 버튼의 콤팩트하고 정밀한 스타일을 절대적으로 따릅니다.
  - **쉐입 (Shape)**: 양쪽 끝이 완전히 둥근 **알약형(Pill) 쉐입**을 적용합니다. (`height: 26px~28px`, `border-radius: 13px~14px`)
  - **아이콘 규칙**: 폰트 리거처 이슈 방지를 위해 **인라인 SVG 사용을 원칙**으로 합니다. 모든 아이콘은 `.lf-icon` 클래스를 포함해야 하며, `stroke-width: 1.6`을 유지합니다.
  - **정렬 (Alignment)**: `display: flex`, `justify-content: center`, `align-items: center`를 통해 아이콘과 텍스트를 버튼의 **기하학적 정중앙**에 완벽히 정렬합니다. 요소 간 간격은 `gap: 8px`, 버튼 내부 패딩은 `padding: 0`을 기본으로 합니다.
  - **레이아웃 전략**: 사이드바와 같이 너비가 좁은 공간(`310px` 이하)에서 여러 기능을 제공해야 할 경우, 한 줄 배치를 지양하고 **2단 스택(Stacked) 레이아웃**을 사용하여 가독성과 클릭 영역을 확보합니다.
  - **컬러 체계 (Hierarchy)**:
    - **Accent (`btn-accent`)**: 핵심 액션 (강조컬러 + 검정/흰색텍스트 + 얇은 테두리)
    - **Primary (`btn-primary`)**: 주요 액션 (어두운 배경 + 흰색텍스트)
    - **Secondary (`btn-secondary`)**: 일반 액션 (밝은 배경 + 어두운텍스트)
  - **상호작용**: 모든 버튼은 호버 시 입체적으로 반응(`translateY(-2px)` + 그림자 강화)하도록 통일합니다.
- **모달 및 팝업**: 시스템 내의 모든 팝업(삭제, 경고, 수정 등) 버튼도 위 디자인 표준을 100% 동일하게 적용해야 합니다.
- **편집 영역**: 에디터 인스펙터와 연동되도록 텍스트 영역에는 `contenteditable="true"` 속성과 `.v4-editable-cell` 클래스를 사용하세요.
- **삭제 트리거**: 사용자가 오브젝트를 삭제할 수 있도록 항상 `.lf-delete-trigger` (`×` 버튼)를 주입하세요.
- **모바일 스마트가이드 오프셋**: Mobile UI의 `mobile-content` 영역에 대한 스마트가이드 타겟 수집 시, 베젤(Inset Shadow) 두께인 **8px 오프셋**을 적용하여 실제 디자인 영역의 경계선에 정확히 스냅되도록 처리해야 합니다.
- **Molecules (컴포넌트 라이브러리) 관리**:
  - **추가(Save)**: 그룹화된 요소를 Molecules에 저장할 때, 이중 래핑 방지를 위해 컨테이너의 `innerHTML`만 저장하고 `width`, `height`, `isGroup` 메타데이터를 별도로 기록합니다.
  - **복원(Load)**: 컴포넌트 삽입 시 저장된 메타데이터를 기반으로 래퍼 크기를 복원하며, 구버전 데이터(Legacy) 호환을 위해 삽입된 HTML 내부에 절대 좌표가 존재할 경우 이를 `0, 0`으로 리셋하는 보정 로직을 반드시 포함해야 합니다.

## 🛡️ 보안 및 통신 규칙 (매우 중요)
- **파일 프로토콜 준수**: `file://` 환경에서는 iframe에 대한 직접적인 DOM 접근(`contentDocument`)이 차단됩니다. 교차 창 간의 모든 비동기 통신에는 반드시 **`MessageHub`** (postMessage wrapping) API를 사용해야 합니다.
- **의존성 인라인화**: `srcdoc` iframe에서의 보안 차단을 방지하기 위해, 핵심 CSS/JS 의존성은 HTML 문자열 내에 직접 인라인으로 주입하세요.
- **명령어 유연성 (Case-Insensitivity)**: `MessageHub`를 통해 전달되는 모든 액션 명령어(예: `ADD_ROW`, `add-row`)는 대소문자를 구분하지 않으며, 하이픈(`-`)과 언더바(`_`)를 모두 수용할 수 있도록 정규화하여 처리해야 합니다.

## 🌐 브라우저 검증 및 경로 규칙 (엄격 준수)
- **로컬 파일 직접 실행**: 브라우저 검증 시 `localhost`나 `127.0.0.1` 같은 가상 서버 환경을 절대 사용하지 마세요. 반드시 **`file:///`** 프로토콜과 절대 경로를 사용해야 합니다.
  - **정상 예시**: `file:///C:/ai-work/viewer.html?project=p_x028c&file=00_Cover_7528.html`
  - **금지 예시**: `http://localhost:3000/...`
- **쿼리 파라미터 유지**: 화면 로딩 검증 시 `project`와 `file` 파라미터가 정확히 포함되었는지 확인하세요.

## ⚠️ 에이전트의 흔한 실수 방지
- **정밀한 분석 후 진행**: 작업을 시작하기 전, 사용자 요구사항과 기존 코드를 픽셀 단위까지 세세하게 분석하고 확인하여 '단 한 번에 확실하게' 진행하세요. 똑같은 이야기를 반복하게 만드는 것은 용납되지 않습니다.
- **사이드 이펙트 방지 (회귀 방지)**: 특정 기능을 수정할 때 다른 멀쩡한 코드(예: 이벤트 리스너, 타 컴포넌트 스타일)를 망가뜨리지 않도록 전체적인 영향도를 반드시 검토하세요.
- **중복 리스너 방지**: `init()` 함수는 파일당 오직 **한 번만** 정의하세요. 여러 번 정의하면 기존 리스너를 조용히 덮어쓰게 됩니다.
- **반응 없는 UI 방지**: `init` 단계에서 모든 UI 요소에 이벤트 리스너를 명시적으로 연결하세요. 특히 `vctrl_inspector.js`에서 `window.DOM`에 등록된 요소가 리스너 없이 방치되는 것은 중대한 결함으로 간주합니다.
- **핵심 기능 상시 검증**: 롤백, 디자인 리뉴얼, 대규모 CSS 수정 후에는 반드시 '스크린 추가(+)', '전체 저장', '사이드바 토글' 등 핵심 UI 기능이 여전히 작동하는지 브라우저 도구로 실검증해야 합니다.
- **병합 시 데이터 유실 방지**: 충돌 발생 시 절대 `metadata.json`을 맹목적으로 덮어쓰지 마세요. 반드시 `screens` 배열을 수동으로 병합(Merge)해야 합니다.
- **SVG 동기화 무결성**: 다이아몬드, 트라이앵글 등 SVG 기반 도형의 스타일 수정 시, `borderColor` 업데이트가 SVG의 `stroke` 속성 및 `1.6` 굵기 표준과 완벽히 동기화되는지 확인하세요.
- **커넥터 화살표 렌더링 (Critical)**: SVG `<defs>` 마커는 `redrawAll` 시 증발할 위험이 크고 색상 상속(`currentColor`)이 불안정하므로, 가급적 **수학적 계산을 통한 Polygon 직접 렌더링** 방식을 권장합니다.
- **사이드바 상호작용 보호**: 사이드바 인스펙터(인풋, 슬라이더 등) 클릭 시 캔버스 요소가 해제되거나 마키(Marquee)가 시작되지 않도록, 전역 `mousedown` 리스너에 `.sidebar` 클래스 체크 가드를 반드시 포함해야 합니다.
- **로컬 캐시 인지**: 엔진(`vctrl_core.js` 등) 수정 후에는 반드시 브라우저의 강력 새로고침(`Ctrl+Shift+R`)을 수행하여 변경 사항이 반영되었는지 확인해야 합니다.


## 🔀 프로세스(플로우차트) 화면 제작 규칙
- **절대 좌표 및 스크롤 방지**: 노드와 연결선은 1440x900 캔버스 내에서 `position: absolute`로 배치하며, 브라우저 스크롤이 발생하지 않도록 레이아웃을 최적화해야 합니다.
- **논리적 분기 및 조건 명시**: 다이아몬드(Diamond) 형태의 조건 분기점(예: 승인 여부, 가용화 등)에는 반드시 'Y'와 'N' 텍스트 노드를 배치하여 조건부 흐름의 방향을 명확히 정의해야 합니다.
- **레이어 및 흐름 구분**: 프로세스의 주체나 시스템(예: LFMall, OMS/SAP, POS 등)이 변경될 경우 가로 점선(Divider)을 사용하여 시각적 구역을 명확히 분리해야 합니다.
- **정밀한 노드 연결**: 직각 화살표(`v4-shape-arrow-elbow`)와 직선 화살표를 혼합하여 사용할 때, 노드와 선이 어긋나지 않도록 픽셀 단위로 정밀하게 위치를 조정해야 합니다.

## 🚀 작업 프로세스 및 안정성 대원칙 (CRITICAL)
모든 작업은 아래의 **5단계 절차**를 엄격히 준수해야 하며, 이를 위반하는 것은 용납되지 않습니다.
1. **고민 (Pondering)**: 요구사항을 깊이 음미하고 시스템 전체에 미칠 영향도를 파악합니다.
2. **분석 (Analysis)**: 기존 코드와 데이터 구조를 픽셀 및 라인 단위로 정밀하게 분석합니다.
3. **설계 (Design)**: 수정할 범위와 방법을 확정하고 사용자에게 승인을 받습니다.
4. **실행 (Execution)**: 설계된 대로만 정확하게 코드를 수정합니다.
5. **확인 (Verification)**: 브라우저와 콘솔에서 에러 여부를 철저히 검증합니다.
   - **[무조건적 원복 규칙]**: 마지막 확인 단계에서 단 하나의 에러(SyntaxError, TypeError, 404 등)라도 발견될 경우, 즉시 모든 작업을 중단하고 **작업 시작 전의 상태로 완전히 되돌립니다(Revert).**

## 🚫 금기 사항 (Anti-Patterns - 절대 금지)
- **임의의 구조 변경 금지**: 사용자의 명시적 승인 없이 폴더 구조를 평면화(Flattening)하거나 하위 폴더를 이동/삭제하는 행위를 절대 금지합니다.
- **전역 데이터 보호**: `data/metadata.json` 등 여러 프로젝트에 걸쳐 영향을 미치는 공통 메타데이터를 임의로 삭제하거나 훼손하지 않습니다.
- **선 검증 후 배포**: 로컬 환경에서 100% 완벽하게 검증되지 않은 코드를 GitHub `main` 브랜치에 푸시(Deployment)하지 않습니다.
- **엔진 무결성 유지**: `vctrl_core.js`, `vctrl_inspector.js` 등 핵심 엔진 수정 시, 모듈 간 호출 함수명(예: `window.updateProperties`)의 일관성을 철저히 검사하여 시스템 마비를 방지합니다.
- **코드 무결성 유지 (Strict Integrity)**: 수정 시 함수 선언부(`const`, `function`), 클래스 정의, 전역 변수 초기화 등 로직의 핵심 구조를 실수로 삭제하거나 누락하지 않도록 극도로 주의하세요. 부분 교체 시 앞뒤 문맥을 철저히 대조하여 구문 에러(`SyntaxError`)를 원천 차단해야 합니다.
- **문서화 언어 규칙**: 사용자 대화와 연동되는 개발 계획서(`implementation_plan.md`), 작업 로그, 주요 기술 문서는 원활한 소통을 위해 **국문(한국어)** 작성을 원칙으로 합니다.
- **사후 보고 의무**: 작업을 완료한 후에는 무엇이 변경되었고, 어떤 검증 과정을 거쳤는지 사용자에게 명확히 보고합니다.
